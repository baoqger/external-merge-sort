#ifndef INTERNAL_SORT_H
#define INTERNAL_SORT_H 

void mergeSort(unsigned int*, int);
void mSort(unsigned int*, unsigned int*, int, int);
void merge(unsigned int*, unsigned int*, int, int, int);

#endif 
